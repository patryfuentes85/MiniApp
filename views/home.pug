doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h1 Welcome to MiniApp!!
        each val in products
            p.searchone__info= `${val.name}`
            img.searchone__img(src=`${val.poster}`)
            p.searchone__type  Type:
            p.searchone__info= `${val.price}`
            p.searchone__type  Desciption:
            p.searchone__info= `${val.description}`

